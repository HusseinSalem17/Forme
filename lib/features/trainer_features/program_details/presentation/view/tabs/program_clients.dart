import 'package:flutter/material.dart';

class ProgramClients extends StatelessWidget {
  const ProgramClients({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}