import 'package:flutter/material.dart';

class WorkoutClients extends StatelessWidget {
  const WorkoutClients({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}